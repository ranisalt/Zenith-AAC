@import 'normalize/normalize';
@import 'bourbon/bourbon';
@import 'neat/neat';

html, body {
	background: #fff;
	font: 13px/1.4em helvetica, sans-serif;
}

a {
	text-decoration: none;
	color: #4183c4;
	
	&:hover { text-decoration: underline; }
}

.clearfix { @include clearfix }
.container { @include outer-container; min-width: 68em; }
.gradient { @include background(linear-gradient(#f9f9f9, #f3f3f3) repeat-x #f3f3f3); }
.right { float: right; }

#content-container {
	@extend .clearfix;
	@extend .container;
	border-bottom: 1px solid #e5e5e5;
	padding: 20px 0 40px;
}
#page-content { @include span-columns(9); }
#page-aside { @include span-columns(3); }
#page-footer { padding: 20px 0; }

#main-nav {
	border-bottom: 1px solid #e5e5e5;
	padding: 6px 0;
	text-shadow: 0 1px 0 #fff;
	
	> .container > div > a {
		color: #333;
		display: inline-block;
		float: left;		
		font-size: 13px;
		font-weight: 700;
		padding: 5px;
		@include transition(all 0.1s ease-in);
		
		&:hover, &.active { color: #4183c4; }
		&+& { margin-left: 5px; }
	}
}

#site-links { float: left; }
#account-links { float: right; }

@import 'forms';
@import 'news';
@import 'account';
@import 'highscore';
