.search-character-form {
	margin: 30px auto;
	width: 400px;
	
	input, a {
		border: 1px solid #ccc;
		border-radius: 3px;
		margin: 5px 0 10px;
		min-height: 34px;		
		padding: 7px 8px;
		
		&:not(.submit) {
			background: #fff;
			box-shadow: inset 0 1px 2px rgba(0,0,0,0.075);
			color: #333;
			margin-right: 8px;
			width: 300px;
			
			&:focus { border: 1px solid #51a7e8; }
		}
		
		&.submit {
			@extend .gradient;
			font-weight: 700;
			padding: 8px 12px;
			text-shadow: 0 1px 0 rgba(255,255,255,0.9);
		}
	}
}

.account-form {
	margin: 30px auto;
	width: 400px;
	
	input {
		border: 1px solid #ccc;
		border-radius: 3px;
		
		&:not([type="radio"]) {
			display: block;
			margin: 5px 0 15px;
			min-height: 34px;		
			padding: 7px 8px;
			width: 100%;
		}
		
		&:not([type="submit"]) {
			background: #fff;
			box-shadow: inset 0 1px 2px rgba(0,0,0,0.075);
			color: #333;
		
			&:focus { border: 1px solid #51a7e8; }
		}
		
		&[type="submit"] {
			@extend .gradient;
			font-weight: 700;
			padding: 7px 12px;
			text-shadow: 0 1px 0 rgba(255,255,255,0.9);
		}
	}
}

.character-form {
	@extend .account-form;
	width: 100%;
	
	.row { @extend .clearfix }
	
	input {
		border: 1px solid #ccc;
		border-radius: 3px;
	}
	
	.name { @include span-columns(8); }
	
	.sex, .vocation, .city {
		padding: 7px 8px;
	
		span { line-height: 34px; }
	}
	
	.title { font-weight: bold; }
	
	.sex {
		@include span-columns(4);
		
		span {
			@include span-columns(5);
			text-align: center;
		}
		.title {
			@include span-columns(2);
			padding-right: 14px;
			text-align: right;
		}
	}
	
	.vocation, .city {
		@include span-columns(6);
		
		.title { text-align: center; }
		span { display: block; }
	}
}
