@extends('public.master')

@section('content')
	<div class='highscore-list'>
		<header>
			<h2 class='rank'>Rank</h2>
			<h2 class='name'>Name</h2>
			<h2 class='level'>Level</h2>
			<h2 class='experience'>Experience</h2>
		</header>
		<section>
			@foreach($characters as $character)
				<div class='rank'>{{{ ++$rank }}}</div>
				<div class='name'>{{ HTML::link(URL::route('character.show', array('skill' => $character->name)), $character->name) }}</div>
				<div class='level'>{{{ $character->level }}}</div>
				<div class='experience'>{{{ $character->experience }}}</div>
			@endforeach
		</section>
	</div>
@stop
