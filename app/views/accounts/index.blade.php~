@extends('public.master')

@section('content')
	<div id='news'>
	@foreach($news as $n)
		<section>
			<header><h1>{{{ $n->title }}}</h1></header>
			<article>{{{ $n->content }}}</article>
			<footer>Posted on {{ date('F jS, Y \a\t h:i A T', strtotime($n->created_at)) }}</footer>
		</section>
	@endforeach
	</div>
@stop
